version: 2.1
jobs:  
  deploy:
    docker:
      - image: 'cimg/ruby:2.6.1'
    steps:
      - checkout
      - run:
          name: apt install
          command: |
            apt-get update \
                && apt-get install -y --no-install-recommends \
                    build-essential \
                    git \
      - run:
          name: install nodejs
          command: curl -sL https://deb.nodesource.com/setup_14.x | sudo bash - &&  apt -y install nodejs
      - run:
          name: install npm
          command: node --version && npm --version
      - run:
          name: install widdershins
          command: npm install -g widdershins
      - run:
          name: add permission
          command: chmod +x deploy.sh
      - run:
          name: deploy slate
          command: ./deploy.sh --gen-widdershins

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - deploy:
          name: deploy slate
          filters:
            branches:
              only:
                - deployCircleCi
